streamlit>=1.28.0
langchain>=0.1.0
langchain-community>=0.0.20
langchain-ollama>=0.1.0
faiss-cpu>=1.7.4
PyPDF2>=3.0.1
cryptography>=41.0.0
ollama>=0.1.7

# SQLite Vector Search (alternative to FAISS)
sqlite-vec>=0.1.6

# Fine-tuning dependencies (CPU versions first, see setup note below)
--extra-index-url https://download.pytorch.org/whl/cu118
torch==2.7.1+cu118
torchvision
torchaudio
transformers>=4.30.0
datasets>=2.14.0
accelerate>=0.20.0
tensorboard>=2.13.0

# IMPORTANT SETUP NOTE:
# For CUDA support, PyTorch must be installed from the CUDA index.
# The --extra-index-url above ensures CUDA versions are found.
# If you have issues, manually install with:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118